# Stage 1: Build the application
FROM maven:3.9-eclipse-temurin-17 AS build
WORKDIR /app
# --- Unneccesary layers going into the final stage
COPY pom.xml .
COPY src ./src
RUN mvn clean package -DskipTests
# ---
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]