FROM ubuntu:22.04

WORKDIR /app

# --- Uneccesary multiple layers

RUN apt update -y && apt apt upgrade -y && apt install -y curl unzip \  
    && curl -L -o my-app.zip https://example.com/releases/app-${APP_VERSION}.zip \
    && unzip my-app.zip

# ---
 
CMD ["./my-app-binary"]
